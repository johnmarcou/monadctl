services:

  monad:
    container_name: monad
    build: .
    privileged: true
    ports:
      - 8080:8080
    environment:
      NODE_ID: 0

  # monad2:
  #   container_name: monad2
  #   build: .
  #   privileged: true
  #   ports:
  #     - 8081:8080
  #   environment:
  #     NODE_ID: 1
